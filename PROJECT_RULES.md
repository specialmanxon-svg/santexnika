# Diyor Group — Муҳандислик ва AI Агент Қоидалари (PROJECT_RULES.md)

Ушбу ҳужжат Diyor Group Dashboard (`diyorgroup.uz` / `santexnika.onrender.com`) лойиҳасида ишлайдиган барча ишлаб чиқувчилар ва AI агентлар (Antigravity, Cursor, Claude ва бошқалар) учун қатъий мажбурий ҳисобланади.

---

## 1. Асосий Архитектура ва Технология Стек

| Қатлам | Технология | Мажбурий қоидалар |
|---|---|---|
| **Backend** | Python 3.11, FastAPI, Uvicorn, Pydantic v2 | Барча эндпоинтлар асинхрон (`async def`), турлар аниқ кўрсатилиши шарт |
| **Маълумотлар базаси** | SQLAlchemy 2.0 (`AsyncSession`), SQLite (`aiosqlite`) / PostgreSQL | База транзакциялари хавфсиз ёпилиши ва сессия ликлари (session leaks) бўлмаслиги шарт |
| **Фон вазифалари** | APScheduler (`backend/workers/task_worker.py`), Celery | Оғир ҳисоб-китоблар сервернинг асосий thread'ини тўсмаслиги керак |
| **Frontend** | Vanilla JavaScript (ES6+), Tailwind CSS (CDN), FontAwesome 6, SheetJS | Битта саҳифали тизим (SPA), ташқи оғир фреймворкларсиз (No React/Vue build step) |
| **HTML Синхронлик** | `index.html` ва `dashboard.html` | **Ҳар доим 100% бир хил (байтма-байт) бўлиши шарт!** |
| **Интеграциялар** | МойСклад JSON API 1.2, Telegram (aiogram 3.31.0), Whisper STT | Лимитлар (5 rps), таймаутлар (10s/30s), кэшлаш ва fallback механизмлари |

---

## 2. Қатъий Ишлаб Чиқиш Қоидалари ва Стандартлар

### 2.1. Backend ва API Эндпоинтлари Хавфсизлиги (Zero-Crash & Graceful Degradation)
1. **Ҳар бир эндпоинтда `try ... except Exception as e`**:
   - Биронта ҳам API эндпоинти frontend'га хом 500 Internal Server Error ёки тушунарсиз хатолик қайтармаслиги керак.
   - Хатолик юз берганда тизим log'га тўлиқ `traceback`ни ёзиши, frontend'га эса қуйидагича graceful fallback қайтариши шарт:
     ```json
     {
       "success": false,
       "error": "Хатолик тафсилоти",
       "data": [],
       "total": 0
     }
     ```
2. **Frontend скриптларини блоклаб қўймаслик**:
   - Backend жавоби кечикса ёки хато берса ҳам, дашборднинг бошқа бўлимлари (KPI, Таҳлил, Омбор, Молия, Ходимлар) музлаб (freeze) қолмаслиги шарт.
   - Ҳар бир frontend fetch сўрови `AbortController` ёки `Promise.race` орқали таймаутга (масалан, 15-20 сония) эга бўлиши керак.

### 2.2. Серверни Қотириб Қўймаслик (Performance & Keep-Alive)
1. **Оғир ҳисоб-китобларни асинхрон фонга чиқариш**:
   - 6 ойлик савдо тарихини тортиш, ABC/XYZ таҳлилини ҳисоблаш, катта Excel файлларни яратиш каби жараёнлар тўғридан-тўғри HTTP сўров ичида синхрон бажарилмаслиги керак.
   - Бундай жараёнлар фонга юборилади ёки `asyncio.to_thread` орқали алоҳида оқимда бажарилади.
2. **Кэшлаш (Caching)**:
   - МойСкладдан олинган маҳсулотлар қолдиғи, савдо тарихи ва контрагентлар маълумотлари хотирада ёки дискда кэшланади (TTL: 5–15 дақиқа).
   - Ҳар бир саҳифа янгиланганда МойСкладга 500-1000 талик сўровларни қайта-қайта юбориш тақиқланади.

### 2.3. МойСклад API Интеграция Қоидалари
1. **Rate Limit чекловлари**:
   - МойСклад API секундига кўпи билан 5 та сўров қабул қилади. Агар лимит ошса (`429 Too Many Requests`), exponential backoff (1s, 2s, 4s) билан қайта уриниш керак.
2. **Таймаутлар**:
   - `httpx.AsyncClient(timeout=httpx.Timeout(10.0, connect=5.0, read=30.0))` кўринишида қатъий таймаутлар ўрнатилиши шарт.
3. **Пагинация**:
   - Маълумотларни қабул қилишда доимо `limit=1000&offset=N` қўлланилиши, бирданига чекловсиз сўров юборилмаслиги керак.

### 2.4. Telegram Бот (aiogram 3.x) ва Овозли Топшириқлар
1. **FSM ва Хатоликлар**:
   - Ботнинг ҳолатлар машинаси (FSM) ҳар доим хавфсиз тозаланиши (`await state.clear()`) ва фойдаланувчи `/cancel` юборганда тизим тўғри ишлаши шарт.
2. **Кўп тиллик ва Whisper STT**:
   - Овозли ва матнли топшириқларни қабул қилишда Ўзбек (лотин/кирилл) ва Рус тиллари таҳлил қилинади, исм-фамилиялар ходимлар базаси билан мослаштирилади.

### 2.5. Frontend UI/UX, CSS Layering ва Z-Index Низоми
1. **Sticky Header контейнери**:
   - Логотип, саҳифа бўлимлари (навигация) ва сана танлаш «Период» блоки `.sticky-top-container` ичида бўлиб, унинг `z-index`и қатъий **1000** (ёки Tailwind `z-40`) деб белгиланган.
2. **Модал ойналар ва оверлейлар (Modals & Drawers)**:
   - Барча модал ойналар (`#orderModal`, `#detailsModal`, `#taskModal` ва ҳ.к.) `z-index`и қатъий **2000** (ёки Tailwind `z-50`) бўлиши шарт. Бу уларнинг sticky header устида тўсиқсиз кўринишини таъминлайди.
   - Модал ойналар тугмаларида `pointer-events: auto` бўлиши, backdrop босилганда ёки `Esc` тугмаси босилганда ёпилиши шарт.
3. **Универсал Пагинация ва ABC/XYZ Фильтрлари**:
   - Барча катта ҳажмли жадвалларда 10, 50, 100 талик пагинация ва [Олдинги / 1, 2... / Кейинги] навигацияси ишлаши шарт.
   - 9 та тоифа (AX, AY, AZ, BX, BY, BZ, CX, CY, CZ) бўйича тезкор фильтр жадвални қайта yuklamasdan (in-memory) тезкор филтрлаши керак.

### 2.6. `index.html` ва `dashboard.html` Тўлиқ Синхронлик Қоидаси
- Лойиҳада иккита параллел файл мавжуд: `index.html` ва `dashboard.html`.
- **Қоида**: `index.html` файлига киритилган ҳар бир ўзгариш дарҳол `dashboard.html` файлига ҳам кўчирилиши керак.
- Файлни сақлашдан олдин ва Git commit қилишдан аввал икки файл орасида фарқ бўлмаслигини текшириш шарт:
  ```powershell
  node -e "const fs=require('fs'); if(fs.readFileSync('index.html','utf8')!==fs.readFileSync('dashboard.html','utf8')){console.error('MISMATCH!'); process.exit(1);} else console.log('OK: HTML files in sync');"
  ```

---

## 3. Ўзгаришларни Текшириш (Verification Checklist)

AI агент ёки муҳандис ўз ишини якунлашдан олдин қуйидаги қадамларни амалга ошириши шарт:

1. **Python Синтаксиси**:
   `.\.venv\Scripts\python.exe -m py_compile <ўзгартирилган_файллари>`
2. **HTML ва JS Синтаксиси**:
   Node.js орқали барча ёзилган скриптлар синтактик жиҳатдан бенуқсон эканлигини текшириш.
3. **HTML Дуал Синхронлик**:
   `index.html` ва `dashboard.html` тўлиқ бир хиллигини текшириш.
4. **Git Статус ва Тартиб**:
   `git status` орқали бегона, кераксиз вақтинчалик файллар қолмаганини текшириш.
5. **Хавфсиз Push**:
   Ўзгаришларни аниқ ва тушунарли commit хабари билан `main` филиалига юбориш.
